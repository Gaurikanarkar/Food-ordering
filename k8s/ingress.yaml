apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: food-ordering-ingress
  namespace: food-ordering
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: food-ordering.imcc.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: food-ordering-service
                port:
                  number: 80
