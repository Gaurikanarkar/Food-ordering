apiVersion: v1
kind: Service
metadata:
  name: food-website-service
spec:
  type: NodePort
  selector:
    app: food-website
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
